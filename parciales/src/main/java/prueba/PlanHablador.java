package prueba;

import java.util.Iterator;
import java.util.List;

public class PlanHablador implements Plan{
	public Double aplicarDescuentosOBonificaciones(List<Llamada> llamadas) {
		Integer cantHorasTotal
		for (Iterator iterator = llamadas.iterator(); iterator.hasNext();) {
			Llamada llamada = (Llamada) iterator.next();
			
		}
	}
	
}
